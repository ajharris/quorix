{
  "name": "Quorix Dev Container",
  "build": {
    "dockerfile": "Dockerfile"
  },
  "postCreateCommand": "pip install -r requirements.txt && npm install --prefix frontend",
  "customizations": {
    "vscode": {
      "settings": {},
      "extensions": []
    }
  }
}
